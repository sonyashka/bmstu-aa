#ifndef SORTING_H
#define SORTING_H

#include <stdio.h>

int *bubbleSort(int *array, int n);
int *insertionSort(int *array, int n);
int *selectionSort(int *array, int n);

#endif // SORTING_H
